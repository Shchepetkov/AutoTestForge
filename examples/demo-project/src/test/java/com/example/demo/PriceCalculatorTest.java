package com.example.demo;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.lang.reflect.Method;
import java.lang.reflect.Modifier;
import java.util.Arrays;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Smoke tests generated by AutoTestForge in offline mode.
 * They pin the public contract of {@link PriceCalculator}; switch to an LLM provider
 * (ollama / openai) for behavioral scenarios.
 */
class PriceCalculatorTest {

    @Test
    @DisplayName("class is loadable and public")
    void class_shouldBePublic() {
        assertThat(Modifier.isPublic(PriceCalculator.class.getModifiers())).isTrue();
    }

    @Test
    @DisplayName("public API declares total")
    void total_shouldBeDeclaredAsPublicMethod() {
        Method[] methods = PriceCalculator.class.getMethods();

        boolean declared = Arrays.stream(methods)
                .anyMatch(method -> method.getName().equals("total"));

        assertThat(declared).isTrue();
    }

    @Test
    @DisplayName("public API declares isEligibleForFreeShipping")
    void isEligibleForFreeShipping_shouldBeDeclaredAsPublicMethod() {
        Method[] methods = PriceCalculator.class.getMethods();

        boolean declared = Arrays.stream(methods)
                .anyMatch(method -> method.getName().equals("isEligibleForFreeShipping"));

        assertThat(declared).isTrue();
    }
}
